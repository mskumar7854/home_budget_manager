name: home_budget_manager
description: A personal budget management app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  sqflite: ^2.2.8+4
  path: ^1.8.3
  # Remove or comment out Firebase dependencies for now
  # firebase_core: ^2.15.1
  # cloud_firestore: ^4.9.1
  # firebase_auth: ^4.7.1
  # flutter_local_notifications: ^15.1.1
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  syncfusion_flutter_charts: ^22.1.16
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true